{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：核心权限 + 系统通知",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
